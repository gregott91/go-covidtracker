package covidtracker

type byDate []DailyCovidData

func (s byDate) Len() int {
	return len(s)
}
func (s byDate) Swap(i, j int) {
	s[i], s[j] = s[j], s[i]
}
func (s byDate) Less(i, j int) bool {
	return s[j].Date.After(s[i].Date)
}
